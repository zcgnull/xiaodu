<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- rectangle(矩形，默认值)
            oval(椭圆，此时corners 节点会失效)
            line(直线，此时必须设置stroke 节点不然会报错)
            ring(圆环)
    -->
    <!--    指定了形状内部的填充颜色-->
<!--    <solid android:color="#DCFBFF"/>-->
    <!--    指定了形状边线的粗细与颜色-->
    <stroke
        android:width="1dp"
        android:color="@color/black"/>

    <!--指定了形状四个圆角的半径-->
<!--    <corners-->
<!--        android:radius="50dp"/>-->
</shape>